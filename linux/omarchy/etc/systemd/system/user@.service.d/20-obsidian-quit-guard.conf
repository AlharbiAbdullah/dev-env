# Omarchy's 10-faster-shutdown.conf gives the whole user session 5s before SIGKILL.
# obsidian-quit-guard needs up to 20s for Obsidian to flush IndexedDB. Only a hang
# pays the extra time; a normal shutdown is as fast as before.
[Service]
TimeoutStopSec=30s
