# Official repo packages beyond what Omarchy 4 already ships. One per line, # comments ok.
# Desktop extras (Omarchy ships hyprland, hypridle, omarchy-shell, ghostty?, chromium, 1password, docker ...)
ddcutil            # monitor brightness over DDC/CI (no backlight on the KTC)
i2c-tools          # i2c-dev module for ddcutil
psmisc             # fuser (news collectors kill stale CDP ports)
wtype
wl-clipboard
grim
slurp
# CLI
bat
btop
eza
fastfetch
fzf
gawk               # ble.sh
git
jq
lazygit
micro
starship
tmux
zoxide
glow
unzip
rsync
bash-completion
# Dev
base-devel
docker
docker-buildx
docker-compose
uv
python-requests
python-yaml
# Net / sec
tailscale
openssh
gnome-keyring      # Chrome cookie encryption + VS Code secrets (must be unlocked by PAM)
libsecret
seahorse           # inspect the keyring
# GTK / portal plumbing the theme switcher uses
glib2
gsettings-desktop-schemas
xdg-desktop-portal-gtk
# Fonts
ttf-noto-nerd      # NotoSansM Nerd Font (Ghostty)
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
ttf-font-awesome
# Apps
obsidian
ghostty            # default terminal (also set via omarchy-install-terminal in step 3)
# AI
ollama-vulkan      # RDNA3 iGPU: Vulkan beats ROCm; needs OLLAMA_IGPU_ENABLE=1
tealdeer          # tldr client, parity with the Mac
atuin             # synced shell history (hosted sync; account + key in 1Password 'atuin sync')

# --- 3.4 hub extras ---
tesseract-data-ara   # Arabic OCR for omarchy-capture-text (OMARCHY_OCR_LANGS=eng+ara in hypr/hyprland.lua)

# --- 3.1 both machines (two-machine audit, installed 2026-09-09) ---
d2                 # diagrams as text, --sketch
vale               # prose linter (style from voice.md, pending)
harper             # harper-ls grammar server
git-delta          # git + lazygit pager
shfmt
shellcheck
duckdb
pgcli
go-yq              # mikefarah yq (pacman `yq` is the Python one)
typst
pandoc-cli
ttf-cascadia-mono-nerd
ttf-ibm-plex       # IBM Plex Sans Arabic
yazi
watchexec
xh
jless
hyperfine
tree
dbeaver            # 3.3 GUI client, Stage 3
amdgpu_top           # iGPU GTT vs VRAM readout; `amdgpu_top -J -n 1` for JSON
smartmontools        # smartd watches the NVMe; alerts via etc/smartd.conf + usr/local/bin/smartd-desktop-alert; enable --now smartd
