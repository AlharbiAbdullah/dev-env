# Minimal two-line prompt, colors driven by the active theme palette.
# The `theme` script updates the `palette = ...` line below on theme switch.

palette = "calma-dark"

format = "$hostname$directory$git_branch$git_status$cmd_duration$character "

add_newline = true

# git can exceed the 500ms default in large repos (e.g. ~/helm), which
# spams [WARN] timeout lines into the prompt.
command_timeout = 2000

[character]
success_symbol = "[\u279c](bold fg:accent)"
error_symbol = "[\u2717](bold fg:red)"

# Remote-session OS icon: one quiet glyph, shown ONLY over SSH (ssh_only keys
# off $SSH_CONNECTION/$SSH_TTY). This config runs on macOS, so it shows the
# Apple glyph; the Linux box uses the Tux glyph. Local sessions show nothing.
[hostname]
ssh_only = true
format = "[\uf179](fg:accent) "

[directory]
style = "bold fg:accent"
truncation_length = 1
truncation_symbol = ""
truncate_to_repo = false
format = "[$path]($style) "

[git_branch]
symbol = " "
style = "fg:yellow"
format = "[$symbol$branch]($style) "

[git_status]
style = "fg:red"
format = '([\[$all_status$ahead_behind\]]($style) )'

[cmd_duration]
min_time = 2000
style = "fg:blue"
format = "[took $duration]($style) "

# -----------------------------------------------------------------------------
# palettes (one per theme; keys must match theme folder names)
# -----------------------------------------------------------------------------

[palettes.calma-dark]
accent = "#d8a657"
yellow = "#d8a657"
red = "#ea6962"
blue = "#7daea3"
green = "#a9b665"

[palettes.calma-light]
accent = "#b47109"
yellow = "#b47109"
red = "#c14a4a"
blue = "#45707a"
green = "#6c782e"

[palettes.everforest]
accent = "#dbbc7f"
yellow = "#dbbc7f"
red = "#e67e80"
blue = "#7fbbb3"
green = "#a7c080"

[palettes.gruvbox]
accent = "#d65d0e"
yellow = "#d8a657"
red = "#ea6962"
blue = "#7daea3"
green = "#a9b665"

[palettes.nord]
accent = "#88c0d0"
yellow = "#ebcb8b"
red = "#bf616a"
blue = "#81a1c1"
green = "#a3be8c"

[palettes.solitude]
accent = "#798186"
yellow = "#d9dbdc"
red = "#565d60"
blue = "#798186"
green = "#9fa5a9"
