# uv-managed CLI tools, same on both machines (uv tool install $(grep -v "^#" uv-tools.txt)).
# Libraries never go here: they are `uv add` inside a project.
marimo
ruff
ty
harlequin[postgres]
sqruff
visidata
